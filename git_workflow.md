# TASK DONE — Xác định đề tài & nguồn dữ liệu

## Branch
`chore/project-scope-definition`

## Quy trình làm việc

1. Checkout branch này trước khi bắt đầu:
   ```bash
   git checkout chore/project-scope-definition
   ```

2. Sau khi hoàn thành, commit và push:
   ```bash
   git add .
   git commit -m "feat: <mô tả thay đổi>"
   git push origin chore/project-scope-definition
   ```

3. Tạo Pull Request về `main` khi xong task.

## Lưu ý
- Không làm việc trực tiếp trên `main`.
- Mỗi commit nên rõ ràng, tập trung vào một thay đổi.
